---
import BaseLayout from '../layouts/BaseLayout.astro';
import Container from '../components/Container.astro';
import AppHeader from '../components/AppHeader.astro';
import HistoryApp from '../components/HistoryApp.astro';
import { defaultLocale, siteConfig } from '../config/site';
import { useTranslations } from '../i18n/ui';

const locale = defaultLocale;
const t = useTranslations(locale);
---

<BaseLayout title={`${t('history.title')} · ${siteConfig.name}`} description={t('site.description')} locale={locale}>
  <Container class="py-4 sm:py-6">
    <AppHeader locale={locale} />
    <HistoryApp locale={locale} />
  </Container>
</BaseLayout>
